# Use relative /api so phone can open via PC LAN IP (Vite proxies to backend).
# Do NOT use http://localhost:5000/api when testing on mobile.
# Mobile camera/GPS need HTTPS: open https://<PC-IP>:5173 (accept cert warning).
VITE_API_URL=/api
